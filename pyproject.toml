[project]
name = "pyclebsch"
version = "0.1.0"
description = "Compute SU(N) Clebsch-Gordan coefficients and other quantities useful for lattice gauge theory simulations"
readme = "README.md"
requires-python = "~=3.14.0"
dependencies = [
    "more-itertools~=10.7",
    "numpy~=2.2",
    "scipy~=1.15",
    "tqdm~=4.67",
]
